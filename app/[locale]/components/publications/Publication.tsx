import "./publication.scss";

type PublicationProps = {
  data: any;
};

export const Publication: React.FC<PublicationProps> = ({ data }) => {
  return <div className="home-page-publications-container"></div>;
};
